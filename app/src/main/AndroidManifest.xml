<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nguye.capston1_dtu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Controller.MainActivity_Chinh"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.List_Image_cong_thuc"
            android:label="@string/formula"></activity>
        <activity android:name=".Controller.List_Image_Bai_Hoc"
            android:label="@string/lesson"></activity>
        <activity android:name=".Controller.ScreenSlidePagerActivity"
            android:label="@string/test"></activity>
        <activity android:name=".Controller.TestDoneActivity"
            android:label="@string/result"></activity>
    </application>

</manifest>